<%= form_with(model: user_session, local: true) do |form| %>
  <% if user_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_session.errors.count, "error") %> prohibited this user_session from being saved:</h2>

      <ul>
      <% user_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :user_session_username %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, id: :user_session_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
